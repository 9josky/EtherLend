import type { Signer } from '../types/signer.js';
import type { PermissionsGrantMessage, PermissionsRevokeMessage } from '../types/permissions-types.js';
import { AbstractMessage } from '../core/abstract-message.js';
export type PermissionsRevokeOptions = {
    messageTimestamp?: string;
    permissionsGrantId: string;
    signer: Signer;
};
export declare class PermissionsRevoke extends AbstractMessage<PermissionsRevokeMessage> {
    static parse(message: PermissionsRevokeMessage): Promise<PermissionsRevoke>;
    static create(options: PermissionsRevokeOptions): Promise<PermissionsRevoke>;
    authorize(permissionsGrantMessage: PermissionsGrantMessage): Promise<void>;
}
//# sourceMappingURL=permissions-revoke.d.ts.map