var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
import { DwnErrorCode } from '../../src/core/dwn-error.js';
import { expect } from 'chai';
import { TestDataGenerator } from '../utils/test-data-generator.js';
import { authenticate, validateAuthorizationIntegrity } from '../../src/core/auth.js';
import { DidResolver, RecordsRead } from '../../src/index.js';
describe('Auth', () => {
    describe('validateAuthorizationIntegrity()', () => {
        it('should throw if given message does not have `authorization` property', () => __awaiter(void 0, void 0, void 0, function* () {
            // create a message without `authorization`
            const recordsRead = yield RecordsRead.create({
                recordId: yield TestDataGenerator.randomCborSha256Cid()
            });
            yield expect(validateAuthorizationIntegrity(recordsRead.message)).to.be.rejectedWith(DwnErrorCode.AuthorizationMissing);
        }));
    });
    describe('authenticate()', () => {
        it('should throw if given JWS is `undefined`', () => __awaiter(void 0, void 0, void 0, function* () {
            const jws = undefined;
            yield expect(authenticate(jws, new DidResolver)).to.be.rejectedWith(DwnErrorCode.AuthenticateJwsMissing);
        }));
    });
});
//# sourceMappingURL=auth.spec.js.map