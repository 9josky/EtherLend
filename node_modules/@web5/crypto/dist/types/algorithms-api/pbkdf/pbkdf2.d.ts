import type { Web5Crypto } from '../../types/web5-crypto.js';
import { CryptoAlgorithm } from '../crypto-algorithm.js';
export declare abstract class BasePbkdf2Algorithm extends CryptoAlgorithm {
    readonly name: string;
    abstract readonly hashAlgorithms: string[];
    readonly keyUsages: Web5Crypto.KeyUsage[];
    checkAlgorithmOptions(options: {
        algorithm: Web5Crypto.Pbkdf2Options;
        baseKey: Web5Crypto.CryptoKey;
    }): void;
    checkImportKey(options: {
        algorithm: Web5Crypto.Algorithm;
        format: Web5Crypto.KeyFormat;
        extractable: boolean;
        keyUsages: Web5Crypto.KeyUsage[];
    }): void;
    decrypt(): Promise<Uint8Array>;
    encrypt(): Promise<Uint8Array>;
    generateKey(): Promise<Web5Crypto.CryptoKey>;
    sign(): Promise<Uint8Array>;
    verify(): Promise<boolean>;
}
//# sourceMappingURL=pbkdf2.d.ts.map